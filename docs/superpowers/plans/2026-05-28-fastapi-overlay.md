# Plan — FastAPI overlay + README contributing section

**Spec:** `docs/specs/2026-05-28-fastapi-overlay-design.md`
**Branch:** `feat/fastapi-overlay` (worktree at `../etude-fastapi-overlay`)
**Target:** PR to `main`

## Assumptions

- `tools/lint_overlay.py` is the canonical linter for `tracks/<parent>/overlays/*.md` and has not changed since v1.3 — verified by `git log --oneline tools/lint_overlay.py` showing no commits after PR #5.
- Skills `assess`/`teach`/`exercise`/`status` already read overlay files (v1.3 wiring). **Untested in the wild** — this overlay is the first real input. *Confirm in step 3.*
- CI runs `lint_overlay.py` on every `tracks/*/overlays/*.md`. *Confirm by reading `.github/workflows/*.yml` in step 1; if missing, file a follow-up issue rather than expand scope here.*
- The four `requires_parent` Python concepts I cite (`type-system`, `typing-generics`, `decorators`, `asyncio`, `testing`, `concurrency`, `dependency-management`, `dataclasses`) all exist in `tracks/python/curriculum.md:8-26` — verified at spec time.
- FastAPI / Pydantic resource URLs from the spec exist today — *verified live in step 2 via WebFetch before commit.*
- No pre-baked `prompt.md` under `tracks/python/overlays/` — exercises are runtime-generated by the `exercise` skill.

## Milestones

### M1 — Pre-flight checks
1. Read `.github/workflows/*.yml`; confirm `lint_overlay.py` runs on overlay files. If not wired, note as follow-up (do not expand scope).
2. Re-read `tools/lint_overlay.py` rules to make sure the planned file shape is compliant (especially: required frontmatter keys, namespacing, prereq validity rules, document-order acyclicity).
3. Verify resource URLs are live (`HEAD` or WebFetch on each).

Exit: written confirmation in the PR description that CI wiring + URLs are confirmed; lint rules cross-checked against the planned file.

### M2 — Write the overlay
Create `tracks/python/overlays/fastapi.md` with the exact frontmatter + 4 modules from spec §3. Run lint locally:
```bash
uv run python tools/lint_overlay.py tracks/python/overlays/fastapi.md
uv run pytest tools/test_lint_overlay.py
```
Both must pass.

Exit: file lints clean, existing tests still green.

### M3 — README contributing section
Insert a new `## Contributing` section before `## Requirements` in `README.md`, ~15-20 lines, per spec §3.B. No other README changes.

Exit: markdown rendering checked locally (or visually inspected); no other section disturbed.

### M4 — Manual skills walkthrough (HUMAN step)
In a scratch dir outside the repo, with the worktree's `tracks/python/overlays/fastapi.md` discoverable:
- `/assess` — confirm FastAPI is listed as an available overlay path on Python.
- `/teach fastapi:routing` — confirm a lesson can be produced (Context7 fetch succeeds).
- `/status` — overlay concept visibility OK.

Any failure here is a skill bug, **not** an overlay bug. File separately; do not patch the overlay to work around the skill.

Exit: human signs off in the PR description.

### M5 — Wrap-up
1. `/claude-md-management:revise-claude-md` — capture anything non-obvious that emerged. Likely candidates: gotchas in the lint schema, skills-awareness verification result, URL fragility patterns. If nothing repo-appropriate emerged, document the no-op explicitly.
2. Open PR to `main`, link spec + plan.
3. After merge: `git worktree remove ../etude-fastapi-overlay`.

Exit: PR merged, worktree removed, memory updated.

## Out of scope (re-stated from spec)

- No change to `tools/lint_overlay.py`, `tools/lint_curriculum.py`, integrity hook, or any skill.
- No pre-baked exercises under `tracks/python/overlays/`.
- No second overlay.
- No `CONTRIBUTING.md` file (README section only).

## Rollback

If lint fails in a way that suggests a schema gap, **do not** touch the linter. Stop, file an issue against v1.3, and rework the overlay file shape to match what the linter actually expects.
