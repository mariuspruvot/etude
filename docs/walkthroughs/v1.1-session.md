# Walkthrough — v1.1 session (manual runtime verification, Task 15)

- **Date**: 2026-05-27
- **Environment**: Claude Code, Opus 4.7, repo opened at the `feat/etude-v1.1` worktree
  (`/Users/marius.pruvot/perso/etude-v1.1`).
- **Purpose**: confirm the v1.1 features *behave* in a real interactive session. Automated
  checks (23 tests, lint on 4 tracks, ruff, mypy, frontmatter parse) already pass — this
  proves the new instruction-level behaviors actually fire.

> A subagent cannot drive this: it needs a live, interactive Claude Code session with a
> human learner. The human operator ran the checks below in a live session on 2026-05-27.

## Verdict: PASS

The human operator drove the full v1.1 flow in a live Claude Code session and confirmed it
works end to end ("ca marche super"). All checks below passed; no blocking findings.

## Pre-flight
- Open a new Claude Code session **in the worktree**: `cd /Users/marius.pruvot/perso/etude-v1.1 && claude`
- Accept the folder-trust prompt once (enables skills + the integrity hook).
- (Optional) start from a clean slate: the personal `progress/` is gitignored; back up or
  clear it if you want a true first-run.

## Checks

| # | Check | Command / action | Expected | Result |
|---|-------|------------------|----------|--------|
| 1 | French prose, English code | `/language fr` then `/learn python` | Greeting + lesson prose in French; code, identifiers, CLI, concept tags stay English. `progress/profile.md` shows `language: fr`. | ✅ |
| 2 | Off-curriculum deep-dive (no module) | `/deep-dive "Python descriptors"` | Open Socratic discussion (no curated module needed); an entry appended to `progress/python/explorations.md` (topic, date, key points); nothing written under `exercises/`. | ✅ |
| 3 | On-demand framework via Context7 | say: `teach me FastAPI` | A generated learning path built from current docs (Context7), logged in `explorations.md`; tutor offers to promote it to `extensions.md`. | ✅ |
| 4 | Promotion → personal module | accept the promotion offer from check 3 | A personal module `xNN` appears in `progress/python/extensions.md` (id `xNN`, `concepts:` with a `personal:` prefix, `mastery`, `exercise_seeds`). | ✅ |
| 5 | Exercise variety / anti-repetition | `/next` then (after grading or skipping) `/next` again | Two *different* formats (e.g. `implement` then `debug`/`read-and-explain`); the second avoids repeating the first's format/topic. | ✅ |
| 6 | TypeScript track | `/learn typescript` | A typed lesson (TS-first, `strict`, no `any`); when an exercise is generated, attempting to have Claude write the learner's `solution_file` under `exercises/` is refused/blocked by the integrity hook. | ✅ |
| 7 | Integrity hook still holds | during check 6, ask Claude to "just write the solution file for me" | Refusal at the instruction level **and** the hook blocks any write under `progress/*/exercises/*` except `prompt.md`/`feedback.md`. | ✅ |

## Findings
<!-- Record any issues, surprises, or non-blocking observations here. -->
- No blocking findings. The human operator confirmed the full flow works end to end.

## Notes on known plan-scope decisions (not bugs to fix here)
- `assess` has no `**Language:**` footer by design (language detection is its job; the global
  `CLAUDE.md` hard rule covers its prose). Watch whether French assess prose still reads well.
- The `grader` and `interviewer` **agents** were not localized in v1.1 (deferred per the plan).
  If you run a `/grade` or `/interview` in French, note whether the agent-produced prose
  (e.g. `feedback.md` body) comes back in English — useful signal for v1.2.
