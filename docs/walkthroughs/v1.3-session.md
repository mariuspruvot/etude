# Étude v1.3 — Manual walkthrough

- **Date**: 2026-05-27
- **Branch**: `feat/etude-v1.3`
- **Result**: **PASS** (driven manually in a fresh Claude Code session inside the worktree).

Verifies the curated-overlay mechanism end to end. Spec:
`docs/specs/2026-05-27-etude-v1.3-overlays-design.md`. Plan:
`docs/superpowers/plans/2026-05-27-etude-v1.3.md`.

| # | Check | Expected | Result |
|---|-------|----------|--------|
| 1 | Lint a valid scratch overlay (`tracks/python/overlays/fastapi.md`, per `tracks/OVERLAYS.md`) | `ok` | PASS |
| 2 | Break it (un-namespaced concept / `requires_parent` typo / forward `oNN` prereq) | linter reports the specific error | PASS |
| 3 | `/learn python` + "is there a FastAPI overlay?" with the scratch overlay present | tutor reads `tracks/python/overlays/*.md`; treats `o01`/`fastapi:routing` as first-class once `parent:async-await` is met | PASS |
| 4 | Grade an overlay-concept exercise | `skills.md` records `fastapi:routing` parent-track-local; never transverse; never skips another track | PASS |
| 5 | Integrity hook | writing under `progress/*/exercises/*` (other than `prompt.md`/`feedback.md`) is still blocked | PASS |
| 6 | Scratch overlay deleted | no overlay content ships in this PR | PASS |

## Automated sweep (controller-run, green)

- `pytest .claude/hooks/` → 17 passed (unchanged)
- `pytest tools/` → 18 passed (9 prior + 9 new `test_lint_overlay.py`)
- `lint_curriculum.py tracks/*/curriculum.md` → ok (go, python, sql, typescript)
- `git diff main -- tools/lint_curriculum.py` → empty (core linter byte-for-byte unchanged)
- overlay lint → no-op (`no overlays to lint`) — mechanism only, no content shipped
- `ruff check` + `ruff format --check` → clean
- `mypy --strict` (incl. `tools/lint_overlay.py`, `tools/test_lint_overlay.py`) → no issues (7 files)
- all `.claude/skills/*/SKILL.md`, `.claude/agents/*.md`, `.claude/commands/*.md` parse

## Notes

- Spec rule 8 (overlay module DAG acyclic) is enforced via document-order prerequisites (a
  forward `oNN` reference is rejected ⇒ cycles are impossible), documented inline in
  `tools/lint_overlay.py`.
- The overlay schema is a NEW, distinct schema; `lint_curriculum.py` and the curriculum
  schema are untouched. Curated overlay CONTENT (FastAPI, React, …) lands in later PRs via
  the promotion path documented in `tracks/OVERLAYS.md`.
