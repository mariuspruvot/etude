# Étude v1.2 — Manual walkthrough

- **Date**: 2026-05-27
- **Branch**: `feat/etude-v1.2`
- **Result**: **PASS** (driven manually in a fresh Claude Code session inside the worktree).

Verifies the v1.2 changes behave end to end. Spec:
`docs/specs/2026-05-27-etude-v1.2-design.md`. Plan:
`docs/superpowers/plans/2026-05-27-etude-v1.2.md`.

| # | Check | Expected | Result |
|---|-------|----------|--------|
| 1 | `/language fr` then a graded exercise | `feedback.md` strengths/improvements in French; `score`, `concept_levels`, code/identifiers in English | PASS |
| 2 | `/interview` in French | warm-up + verbal questions + debrief in French; `solution_file` name + code in English; hand-off to `grader` works | PASS |
| 3 | Deep-dive the same niche twice | promotion to `extensions.md` is offered only on the 2nd visit, never imposed | PASS |
| 4 | `/revise` after an ungraded deep-dive | a topic not revisited in > 30 days surfaces as a light recall; a `learning` concept with `last_graded: —` is offered | PASS |
| 5 | Integrity hook | writing under `progress/*/exercises/*` (other than `prompt.md`/`feedback.md`) is still blocked | PASS |

## Automated sweep (controller-run, green)

- `pytest .claude/hooks/` → 17 passed (unchanged)
- `pytest tools/` → 9 passed (6 lint + 3 new `test_agents_language.py`)
- `lint_curriculum.py tracks/*/curriculum.md` → ok (go, python, sql, typescript)
- `ruff check` + `ruff format --check` → clean
- `mypy --strict` (incl. `tools/test_agents_language.py`) → no issues
- all `.claude/skills/*/SKILL.md`, `.claude/agents/*.md`, `.claude/commands/*.md` parse

## Notes

- The v1.3 curated-overlay design (`docs/specs/2026-05-27-etude-v1.3-overlays-design.md`)
  ships as a design doc only — not implemented in this branch (scope lock honored).
